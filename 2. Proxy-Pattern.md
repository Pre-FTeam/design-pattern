_구조패턴 : 클래스나 객체를 조합해 더 큰 구조를 만드는 패턴_
# 💡 정의
- 실제 구현체를 바로 사용하지 않고 대리자 객체(Proxy Object)가 클라이언트의 요청을 대신 실제 구현체에 전달하며 처리하는 구조 패턴이다.


# 프록시 패턴은 왜 사용될까?

프록시 객체를 사용하면 실제 객체로 바로 가지 않고, 프록시 객체에게 전달되고, 프록시 객체가 실제 객체에게 전달하게 된다.

그렇다면 왜 실제 객체에게 바로 가지 않게 하는 걸까? 🤔

**1. 실제 객체가 존재하지 않을 경우**
   - 크기가 매우 커 무거운 객체는 생성하는 데에 오래 걸린다.
   - 프록시 객체가 요청을 대신 받아 객체 생성 후에 요청을 전달하여 처리가 가능하다.

**2. 실제 객체에 대한 접근 제어가 필요한 경우**
   - 프록시 객체가 대신 요청을 전달하기 때문에 흐름을 제어할 수 있다.
   - 동시다발성 요청에 의한 오류를 지연 초기화를 통해 막을 수 있다.
   - 프록시 객체는 대신 요청을 전달하는 것을 통해 접근 제어가 가능하다.

**3. 실제 객체 접근 전 후에 작업이 필요할 때**
   - 프록시 객체는 실제 객체를 생성, 호출하기 전에 특정 로직을 추가할 수 있다.
   - 특정 클라이언트에게만 접근을 허용하는 등의 흐름을 담당할 수 있다.

# 프록시 패턴의 구현 방법
![img.png](https://refactoring.guru/images/patterns/diagrams/proxy/structure-indexed-2x.png)
_출처: [refactoring.guru](https://refactoring.guru/ko/design-patterns/proxy)_
1. 실제 서비스를 위한 인터페이스를 선언한다. 프록시와 실제 구현 객체 모두 이 인터페이스를 따라야 한다.
2. 인터페이스를 따라 구현된 실제 객체는 서비스를 위한 실질적인 로직을 가진다.
3. 프록시 클래스는 요청을 받으면 (필요하다면) 캐싱, 액세스 제어, 지연 초기화 등등의 작업을 처리한 후 실제 객체에 전달한다.
4. 클라이언트는 인터페이스에 의존하도록 하며, 인터페이스에 요청을 보내고 결과를 전달받게 된다.

# 프록시 패턴의 장점
- OCP(개방/폐쇄 원칙)를 준수한다.
  - 클라이언트를 변경하지 않고 프록시를 통해 문제를 해결 가능하다.
- 서비스 객체(실제 객체)가 준비되지 않아도 작동이 가능하다.
  - 실제 객체가 생길 때까지 기다릴 수 있다.
- 실제 객체의 메소드 호출 전/후에 특정 로직을 추가하여 기능 확장이 가능하다.
  - 실제 서비스 데이터에 대한 변경을 일으키는 로직은 안된다.
  - 제어의 흐름만 담당하는 로직만 추가 가능하다.


# 프록시 패턴의 단점
- 항상 프록시 객체를 거쳐야 하기 때문에 성능이 저하될 수 있다.
- 많은 개체를 위해 프록시 패턴이 적용되는 경우 코드가 복잡해질 수 있다.

# 프록시 패턴 VS 데코레이터 패턴
- 프록시 패턴은 접근을 제어함을 통해 프록시 객체가 흐름을 전반적으로 담당하도록 하는 디자인 패턴이다.
- 데코레이터 패턴은 여러 객체를 조합하여 기능을 확장할 수 있도록 지원하는 디자인 패턴이다.

# Reference
- https://refactoring.guru/ko/design-patterns/proxy
- https://esoongan.tistory.com/180